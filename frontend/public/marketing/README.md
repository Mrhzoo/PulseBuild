# Marketing landing assets

Drop real files here if you have them:

- `assets/logo.webp` — circular PulseBuild mark
- `fonts/GeistPixel-Circle.woff2` — Geist Pixel Circle

Until then the page uses SVG initials + Inter / system fallbacks.

Background video is a public aerial loop (CloudFront-compatible CDN). Swap the `<source>` in `index.html` when you have a branded reel.

Open: `http://localhost:3000/` (Next rewrite) or `/marketing/index.html`.
