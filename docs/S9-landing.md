# S9 Marketing landing

Landing is static HTML (no React) at `frontend/public/marketing/`.

- Open `http://localhost:3000/` — Next serves an iframe of `/marketing/index.html`
- Digest app moved to `/app`
- Sign in / Get Started → `/login` (then `/app`)

Replace `assets/logo.webp` and `fonts/GeistPixel-Circle.woff2` when you have licensed files. Background video has a CloudFront source plus a Pexels aerial fallback.

Honest copy only. No fake enterprise counts.
