# S13 Digest dashboard

`/app` loads `GET /api/digest/today`.

- Act then Watch then quiet chips then coaching ask
- Owner/Ops: Send, Refresh, Flag, Dismiss
- Reader: view only
- Empty morning links to `/app/projects` (S14 placeholder)
- Flag success exposes Copy share link from `share_url`

No invented cards. Email remains the SLA line from the payload.
