# S12 Marketing + pricing

Pages:
- `/product` ingest, agents, digest
- `/case-studies` illustrative Marina / IPC / variation (labeled examples)
- `/contact` POST `/api/contact` writes `data/contact/`
- `/pricing` Pilot AED 1,500 / month (3 projects) + pack AED 400 / project / month

CTAs: logged out `/login?next=/billing`, logged in `/billing`.

Landing nav updated. Theme/locale on marketing header.

Override display with `NEXT_PUBLIC_PRICE_PILOT` and `NEXT_PUBLIC_PRICE_PACK`.
