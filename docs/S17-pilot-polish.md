# S17 Pilot polish

## Demo smoke
1. Phone width: login → digest quiet → Upload → `/app/projects`.
2. Create project → upload → Run agents → digest Act/Watch or honest quiet.
3. Flag → `/app/flags` → copy → revoke → share unavailable.
4. `/app/billing` stub checkout → `?session=` no charge.
5. Settings Meta checklist: all manual / configure in env. Email SLA line visible.

## Polish
- Touch ≥44px, focus ring, wrap cards, burger ≤640px
- Duplicate `flag_finding` removed from `routes.py`
- Redirects `/flags` `/billing` `/onboarding` still work
