# S28 Morning Command + motion finish

- Landing: section-scrubbed morning study; crane + programme-board cut-out (no PLACEHOLDER).
- `/app` is Act inbox + Watch rail + Activity (`GET /api/activity`).
- Days flagged from Act **and** Watch text.
- Locale keys for nav, landing, command. Invite gate still S29+.
