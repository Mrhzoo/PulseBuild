# Golden fixtures

Keep anonymized examples here as the product grows:

- one programme / delay letter
- one IPC or payment PDF
- one change-order email
- one thin/empty project

Gate: Act evidence rate ≥ 95% (`GET /api/eval/golden` or `pytest`).
